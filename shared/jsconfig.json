// File so all backend files are mapped together, so any rename or move allows an automatic references change on any other file
{
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext"
  },
  "exclude": ["node_modules"]
}
